import { expect } from 'chai';

describe('Template', () => {
  describe('sample test', function() {
    it('should pass', async () => {
      // Assemble/Arrange
      const val = true;
      
      // Assert
      expect(val).to.be.true;
    });
  });
});